import 'package:flutter/material.dart';

class LandingTabletPage extends StatelessWidget {
  const LandingTabletPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Tablet")));
  }
}
